## Deploying the V-shop

Key-cert should work with `nginx.conf` but better to create your own CA and certs.

1. Do `docker compose up -d` in this directory.
2. When finished, do a `docker compose down`.